run: uvicorn app:app --reload
seed: python seed.py
test: pytest -q
base_url: http://localhost:8000
endpoints:
  - POST /admin/widgets
  - GET /widgets/{widget_id}/config
  - GET /widget.v1.js
  - OPTIONS /submissions
  - POST /submissions
  - GET /dashboard/stats
